services:
  postgres:
    image: postgres:16.11
    container_name: vocalis-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: vocalis
      POSTGRES_USER: vocalis
      POSTGRES_PASSWORD: vocalis
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - vocalis-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vocalis"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Backend service (uncomment when ready to run in Docker)
  # backend:
  #   build:
  #     context: ./backend
  #     dockerfile: Dockerfile
  #   container_name: vocalis-backend
  #   restart: unless-stopped
  #   ports:
  #     - "8080:8080"
  #   environment:
  #     SPRING_PROFILES_ACTIVE: dev
  #     DB_HOST: postgres
  #     DB_PORT: 5432
  #     DB_NAME: vocalis
  #     DB_USER: vocalis
  #     DB_PASSWORD: vocalis123
  #     JWT_SECRET: your-256-bit-secret-key-here
  #     UPLOAD_DIR: /app/uploads
  #   volumes:
  #     - ./uploads:/app/uploads
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #   networks:
  #     - vocalis-network

  # Frontend service (uncomment when ready)
  # frontend:
  #   build:
  #     context: ./frontend
  #     dockerfile: Dockerfile
  #   container_name: vocalis-frontend
  #   restart: unless-stopped
  #   ports:
  #     - "3000:3000"
  #   environment:
  #     NEXT_PUBLIC_API_URL: http://localhost:8080/api
  #   depends_on:
  #     - backend
  #   networks:
  #     - vocalis-network

  # pgAdmin (optional - for database management)
  pgadmin:
    image: dpage/pgadmin4
    container_name: vocalis-pgadmin
    restart: unless-stopped
    ports:
      - "5050:80"
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@vocalis.local
      PGADMIN_DEFAULT_PASSWORD: admin123
    volumes:
      - pgadmin_data:/var/lib/pgadmin
    networks:
      - vocalis-network
    depends_on:
      - postgres

networks:
  vocalis-network:
    driver: bridge

volumes:
  postgres_data:
  pgadmin_data:
